sequenceDiagram
    participant browser
    participant server

    Note over browser: The user writes a note and clicks the "Save" button

    Note right of browser: The JavaScript code intercepts the form submit, appends the new note to the local list dynamically, and redraws the view instantly 

    browser-->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note over server: The server receives the JSON data payload and pushes it into the data array
    server-->>browser: HTTP status code 201 Created (JSON confirming success)
    deactivate server

    Note right of browser: No page refresh occurs; the updated list is already visible to the user.
